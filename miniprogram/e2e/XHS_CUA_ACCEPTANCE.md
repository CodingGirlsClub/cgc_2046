# 小红书端 CUA 端到端验收报告

- 日期：2026-09-26
- 分支 / 构建：`swift-sparking-quartz` @ `1bc3daa7`（P2 闪念间全家桶头）
- 执行方式：Orca Computer Use 驱动小红书开发者工具模拟器（无人手兜底全程）
- 结论速览：**不能提审**。P0 缺陷 1 个（登录链路真机未验通），其余大量 Blocked 等真机。

## 环境

| 项 | 值 |
| --- | --- |
| 开发机 | macOS（arm64），小红书开发者工具 V2.9.1 |
| 模拟器 | iPhone 14 Pro / 自适应 / WiFi，基础库 3.102.3（project.xhs.json） |
| AppID | `<小红书appid>`（工具项目配置既有值） |
| 后端 | 本地 dev `http://localhost:4001/api/graphql`（worktree 独立库 `cgc_2046_dev_swift_sparking_quartz`） |
| 测试数据 | 种子脚本一次性灌入：免费场 `xhs-accept-free`、收费场 `xhs-accept-paid`、押金场 `xhs-accept-deposit`（均 open/public/报名开放中）；闪念间 `2014-01-11-bj` 档案 + Person（测试邮箱 `xhs-acceptance@example.com`）+ 已发布金句 + 已挂树公开愿望 |
| 上传版本 | **0.2.3**（构建 `1bc3daa7`，含 SourceMap；截图 `ENV-01`）。**尚未设为体验版**——需管理员在开放平台管理后台操作 |
| 测试账号 | 无独立测试账号 A/B；模拟器使用工具已登录小红书账号（手机号 131\*\*\*\*1490 打码显示，见 A1-04） |

## 执行环境限制（影响多项判定，先读）

1. 模拟器交互在授权弹窗「允许」一次点击后全部失效（orca 合成坐标点击不再被工具 webview 响应；原生工具栏按钮的 AX 点击始终有效）。重启工具/helper 均复现。因此**除发现页与登录页外无法导航**。
2. 账号 A（纯新账号）/账号 B（已绑定档案）未由 owner 提供；模拟器内使用工具登录态的单账号。
3. 真机（iOS）不可达：无设备配合。

以下判定以「实际看到什么」为准；无法操作到位的一律 Blocked，未做推断。

## A. 登录链路（P0-1 修复真机首验）

- **A1 首次登录 — Fail**。游客态发现页 → 「去登录」→ 登录页（内联授权说明 + 主按钮，`A1-02`）→ 点「手机号快捷登录」→ 弹隐私授权说明（`A1-03`）→ 「同意并登录」→ **平台手机号授权弹窗正常出现**（131\*\*\*\*1490，`A1-04`）→ 「允许」→ **「Platform sign in failed」**，留在登录页（`A1-05`）。重试 1 次复现同样失败（第二次点击后疑似 `Taro.login()` promise 挂起，按钮不再响应）。
- **A2 回访静默登录 — Blocked**。前提（首次登录成功）不存在。
- **A3 退出登录 — Blocked**。同上，无已登录态。
- **A4 失败取证 — 完成**（记录，不修代码）：
  - 后端（4001）全程 **零登录请求**：无 `signInWithPlatform` mutation、无 `POST /api/rmp/token` 外呼。失败发生在**前端平台 API 层**（`preparePlatformLogin` 首步 `await Taro.login()` 即 reject，message = `Platform sign in failed`），**P0-1 的三项修复（token POST、AES 解密、小程序码）本次完全未被执行到**。
  - 调试器 Console 无登录相关错误输出（仅渲染层 deprecation 警告 + BUG-1 的 RUNTIME ERROR）。
  - 待排除（真机/平台后台）：① 模拟器是否根本不支持 `xhs.login`；② 该 appid 的登录/手机号权限配置；③ `@tarojs/plugin-platform-xhs` 1.2.2 的 login 事件透传（checklist Q4）。**体验版真机扫码是下一步**。

## B. Tab 结构与「我的」

- **B1 底部 Tab — Pass（部分）**。AX 树与多张截图（`A1-01` 等）确认恰好三个：发现 / 闪念间 / 我的，顺序正确；发现页选中态高亮正常。闪念间/我的两个 Tab 的点击切换未验（交互通道失效）。
- **B2 「我的」页 — Blocked**。无法切 Tab；未登录态表现也未观察到。
- **B3 隐私政策页 — Blocked（有旁证）**。登录弹层中《隐私授权说明》为可点链接样式（AX 树确认存在，`A1-03`）；点击无响应（环境交互失效，无法定责产品）；隐私页正文渲染未观察到。

## C. 报名漏斗与缴费门（D1a）

- **C1 免费场报名 — Blocked**。需登录 + 详情页导航，均不可达。
- **C2 收费/押金场阻断文案 — Blocked（部分旁证 Pass）**。发现页列表层已展示收费场「小红书验收·押金场 开放报名 报名中」（`A1-01`），列表层无任何支付引导字样；详情页阻断文案未观察到。
- **C3 register-form 深链阻断 — Blocked**。
- **C4 订阅触点清零 — 部分通过**。已见 5 个页面（发现/登录/授权弹层/授权弹窗/错误态）AX 树全文无「订阅消息/订阅提醒/订阅」字样；「我的报名」底部提示文案未观察到（Blocked 部分）。
- **C5 邀请码 — Blocked**。

## D. 闪念间全家桶

- **D1–D7 全部 Blocked**。第二 Tab 无法点击切换；金句墙/许愿树/写愿望/找回/相册/今天的你/分享面板均未到达。渲染红线（无白屏/无 canvas 崩溃）在已见页面成立，全页面未验。

## E. 深链与平台过滤

- **E1 / E2 — Blocked**。深链需分享卡或扫码入口，模拟器内无法构造。

## F. 平台合规抽查

- **F1 站外引导抽查 — 部分通过**。已抽查 5 个页面截图（`A1-01` 发现、`A1-02` 登录、`A1-03` 授权弹层、`A1-04` 授权弹窗、`A1-05` 错误态），AX 文本与截图双重确认均无「微信/网页端/公众号/二维码/加我/扫码」字样。动态深层流程（报名、附议、写愿望等）未抽查到。
- **F2 首屏性能 — Pass（仅模拟器口径）**。后端就绪后冷启动编译完成到发现页数据渲染秒级完成（构建日志 19:47:51 完成编译，Catalog 查询随即 200 返回 3 场活动）。真机口径待验。
- **F3 iOS 真机付费入口 — Blocked**。无真机。

## 缺陷清单

| # | 严重级 | 缺陷 | 复现 / 预期 vs 实际 | 证据 |
| --- | --- | --- | --- | --- |
| 1 | **P0**（待真机定责） | 首次登录失败：`Taro.login()`（xhs.login）reject，错误 `Platform sign in failed`，后端零请求 | 发现页 → 去登录 → 同意并登录 → 允许手机号授权；预期落「我的」登录态，实际报错留在登录页；重试复现 | `A1-04`、`A1-05`、后端日志（无 signInWithPlatform / rmp/token） |
| 2 | P1 | 登录页 mount 触发 RUNTIME Service ERROR：`onShareTimeline 或 onShareChat 必须与 onShareAppMessage 配合使用`（`showMenuByShareApi`） | 打开登录页即现于 Console；预期无平台 API 报错；实际分享钩子配置不满足平台约束，该页分享能力不可用/审核风险 | `BUG1-share-runtime-error-console.png` |
| 3 | P2 | 登录失败文案直出英文平台错误 `Platform sign in failed`，未映射为用户可读中文（`error-copy.ts` 兜底缺失或平台错误未归一） | 同缺陷 1 复现路径；预期中文可恢复提示，实际英文裸错误 | `A1-05` |
| 4 | P2 | 编译警告 `[xhs warning] Template tmpl_0_5 is being called recursively in base.xhsml` ×4 | 打开调试器 Console 即现；预期无模板递归 | 同 BUG1 截图右侧 Console |
| 5 | P3 | 登录页授权说明含「后续通知仍需你逐次授权」——小红书端无订阅消息能力（P0-2 已下线），「通知」表述语义悬空易误导 | 阅读登录页文案即现 | `A1-02`、`A1-03` |
| 6 | 观察项 | 登录弹层《隐私授权说明》链接点击无响应（本环境交互通道失效，无法定责；真机复验） | 弹层内点链接无跳转 | `A1-03` |

## Owner 结论（≤5 行）

1. **不能提审**：登录链路（P0 首验项）在模拟器即失败，且 P0-1 三项修复代码路径本次一行都没跑到。
2. P0 缺陷 1 个（缺陷 #1），P1 一个（分享钩子平台报错，审核风险）。
3. 最危险发现：`xhs.login` 在平台 API 层静默失败、console 无错误——若真机同样失败，整端不可用且难排查。
4. 已上传构建 **0.2.3**（未设体验版）→ 请在开放平台管理后台设为体验版，真机扫码复跑 A 组 + D 组。
5. 需 owner 提供：测试账号 A/B、该 appid 的登录/手机号权限确认、开发者工具交互失效时的人工兜底。
