# direnv：进入 backend 自动加载 .env（真实值不入 git；样例见 .env.example）
# 首次使用：cp .env.example .env && 填值 && direnv allow
# set -a + source：direnv 内建 dotenv 逐行解析、不支持多行值（PEM 私钥），
# bash source 原生支持；set -a 让所有赋值自动 export，子进程可见
watch_file .env
set -a
source .env
set +a
