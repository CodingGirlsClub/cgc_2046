# 技术调研与实施计划(端到端 TDD)

> 调研日期:2026-07-31 · **修订:2026-08-01(按 grill 决策 D1/D5/D6/D7/D8/D9/D12/D13/D14)** · **2026-08-01 workflow-first 同步版(按 D-A1–D-A7 / ADR-0002)**
> 范围:后端 Elixir/Phoenix/Ash 生态 + 前端 Next.js/Apollo + OpenClacky 连接器扩展(cgc-2046)
> 本文件回答:**每个功能该用哪些依赖 / 哪些自己写 / 社区最佳实践是什么**,并按端到端 TDD 方式给出实施计划。
> **M2 重大修订**:删除 ash_ai 路线(D1);网站不再跑 LLM/ToolLoop,改为暴露 **MCP server(anubis_mcp)**(D5/D6)+ **连接器扩展(cgc-2046)**(D14)路线。
> **workflow-first 同步(D-A1–D-A7 / ADR-0002)**:新增 Jido 生态引擎(WorkflowDefinition/WorkflowRun 核心)、业务 workflow 切片(报名/赞助/邀请)、partition 多租户与 Thread journal 审计;决策依赖:docs/03-决策记录/grill-决策记录-2026-08-01.md、docs/01-定稿设计/领域模型定稿.md、docs/adr/0002-workflow-first-jido.md。

---

## 一、技术栈决策总览

### 1.1 后端(Elixir / Phoenix / Ash)

| 领域 | 决策 | 依赖/方式 | 一句话理由 |
|---|---|---|---|
| 多租户 | **用官方 attribute 策略** | `ash_postgres` 内置 `Ash.Postgres.DataLayer` 多租户 | 租户即 `workspace_id` 列过滤,配 `Ash.PlugHelpers.set_tenant/2`,无需 Postgres schema 隔离的运维复杂度 |
| RBAC | **自研** | 手写 `policies` + `Cgc2046.Rbac` 模块 | `ash_rbac` 的角色是**编译期 DSL 写死**的,与"租户内可扩展 Role 数据库实体 + 多角色并集"不匹配 |
| 认证 | 用官方 | `ash_authentication` 4.14(Password 策略 + TokenResource) | 社区标准;GraphQL 自动生成 `signIn`/`signUp` mutation 和 `getUser` query |
| MCP server(网站暴露) | **用 anubis_mcp** | `anubis_mcp`(Elixir/Phoenix,活跃维护) | 网站暴露**一个** MCP server 被用户 OpenClacky 调用(D5/D6);不再自行实现 Agent/LLM(D1) |
| Agent 执行(网站侧) | **不做** | 无 —— 由用户本地 OpenClacky 执行 | 网站零 AI 成本(BYO,D3);用户侧经 MCP 调用网站工具 |
| 连接器扩展(用户侧) | **自研扩展 cgc-2046** | OpenClacky 扩展(Ruby api / JS panels / Markdown skills) | 用户侧统一入口:读 mcp.json、同步工作区 Skill、内置 CGC 助手(D10/D11/D13/D14) |
| Workflow 状态机 | 用官方(限定范围) | `ash_state_machine` 0.2.13 | WorkflowDefinition 生命周期(draft→published→archived)与资源级状态用声明式状态机;**WorkflowRun 状态机(pending→running→waiting→succeeded/failed/cancelled)由 Jido 引擎承载**(D-A1) |
| Workflow 引擎(workflow-first) | **用 Jido 生态** | `jido` + `jido_runic` + `ash_jido`(+ jido_signal:CloudEvents) | workflow 核心 = **WorkflowDefinition(蓝图)/ WorkflowRun(实例)**(D-A1);Runic alpha 锁版本 `~> 0.1.0` + 适配层隔离(ADR-0002) |
| workflow 多租户 | **Jido partition** | partition(registry identity/persistence/lineage/telemetry 隔离) | **每 Workspace = 一个 partition**(D-A5);WorkflowRun 归属 partition |
| workflow 审计 | **Jido Storage Thread journal** | Thread(append-only)+ Checkpoint + Introspection 溯源链 | 引擎审计流(D-A5);与 ToolCallLog(MCP 接入审计)/AgentRun(按 Step 聚合)互补 |
| 软删除 | 用官方 | `ash_archival` 2.0.3 | 一处配置全资源生效 |
| 工具调用审计 | **v1 自研 + 二期 paper_trail** | 自研审计中间件/资源 + `ash_paper_trail` 0.6.0(二期) | 每次 MCP 工具调用 = 审计记录(v1,D6/D9);通用变更留痕二期 |
| API 层(网站 UI) | 用官方(已选) | `ash_graphql` 1.10 | 已定稿,不再比较 |
| API 层(MCP) | **用 anubis_mcp** | anubis_mcp 的 MCP endpoint(JSON-RPC over HTTP/SSE) | 网站即 MCP server;工具内部复用 Ash actions + 同一授权链(D6) |
| 邀请 token | 用官方工具 | `AshAuthentication` Confirmation / 自生成 token 存 hash | 见 §3.4 |

### 1.2 前端(Next.js 16 App Router + Apollo Client 4)

| 领域 | 决策 | 说明 |
|---|---|---|
| 渲染模式 | **全客户端渲染** | `"use client"` + `ApolloProvider` 在 root layout 包裹(RSC 只做壳)。平台是重交互应用(工作台/审批/审计/引导),SSR 收益低、复杂度高。**不引入** `@apollo/client-react-streaming` |
| 跨域 | **Next.js rewrite 代理** | `next.config.ts` 加 `rewrites()`:`/api/graphql` → `http://localhost:4000/api/graphql`,消除 CORS;后端 cors_plug 保留作双保险 |
| 认证传输 | **token 存 httpOnly cookie + Authorization header** | 登录 mutation 返回 JWT → 写 cookie → Apollo link 中间件读取附加 `Authorization: Bearer` |
| 页面形态 | **形态 X(D4)** | **无对话页 / 无执行页**;页面 = 工作台 / 成员角色 / Workflow 产出展示 / 审批 / 审计查看 / 连接引导;聊天全在用户自己的 OpenClacky |
| 状态管理 | **只用 Apollo cache** | 不引 Redux/zustand;需要再拆 |

### 1.3 明确不用的(调研排掉)

- `ash_rbac`:角色编译期写死,不匹配数据库 Role 实体 → 自研
- `ash_audit_log`:包不存在 → 二期用 `ash_paper_trail`
- `ash_ai` / `ash_oban`(网站侧):**删除(D1)**——网站不再跑 LLM/ToolLoop;AgentRun 由 MCP 工具调用自动生成(D9),无需 Oban 后台执行 LLM
- `ash_agent` / `ash_agent_session` / `ash_agent_tools`(第三方 bradleygolden):非官方、不成熟 → 本架构已不需要
- `ash_ai.gen.chat` 生成器:随 ash_ai 一并删除;网站无对话流式需求
- `ash_json_api` / `ash_csv`:已选 GraphQL
- GraphQL subscriptions:实验性且依赖 Absinthe 上下文 → 不用(网站无 Agent 对话流)
- **"每工作区一个 MCP server"**:工具名合并冲突 → 单 MCP server + 必填 workspace_id(D12)
- **运行时下发 Agent 文件**:热加载未验证、工程风险高 → 任务指令模式(D10)
- **其他 workflow 引擎(Temporal / BPMN / 自研 DAG)**:不引入——Jido 生态已选(D-A1);Runic alpha 通过锁版本 + 适配层兜底(ADR-0002)
- **业务侧反向调引擎**:禁止——依赖方向恒为 workflow → 业务 Action 接口;业务 context 反向只发 Signal、不调引擎(D-A1/ADR-0002)

---

## 二、关键调研结论(带证据)

### 2.1 Ash 多租户 + MCP 作用域(hexdocs ash/multitenancy 已读 + D12)

- **attribute 策略**:租户资源加 `multitenancy strategy: :attribute, attribute: :workspace_id`;所有查询自动按 tenant 过滤。
- **set_tenant 必须在认证 plugs 之前**(`Ash.PlugHelpers.set_tenant(conn, tenant)`),因为 User/Token 校验需要正确的租户上下文。
- **全局资源**(User / Workspace)不带多租户;租户资源(User 之外的业务资源)带 `workspace_id`。
- Code interface / action 内可用 `Ash.Query.set_tenant/2`、`Ash.Changeset.set_tenant/2`;`Ash.Scope` 可打包 actor/tenant/context。
- **两个 Api 模块**:`Cgc2046.Api`(租户资源,默认要求 tenant)+ `Cgc2046.GlobalApi`(User/Workspace)。
- **MCP 作用域(D12)**:OpenClacky 的 MCP client 是 **server 级全局长连接**(进程级共享)→ 不能存服务端会话状态(会跨会话串)。因此所有 MCP 工具带**必填 `workspace_id`**(无状态、每调用鉴权 + 审计);"当前工作区"由用户侧 CGC 助手维护。

### 2.2 anubis_mcp(网站 MCP server,替代 ash_ai)

- **定位**:网站暴露**一个** MCP server(Elixir/Phoenix,活跃维护);用户的 OpenClacky 以 `type: http` 连接(`url: https://cgc.example.com/mcp`,D13)。
- **协议**:MCP(JSON-RPC 2.0 over HTTP + SSE);工具经 `tools/list` / `tools/call` 暴露;工具内部调用 Ash actions,复用同一授权链(D6)。
- **认证**:每用户连接 token 放 `Authorization: Bearer <token>` 头;token 绑**用户不绑工作区**(D13),租户由每次调用的 `workspace_id` 参数判定(D12)。
- **与 GraphQL 的关系**:GraphQL 供网站 UI;MCP 供用户 OpenClacky。两者共享同一批 Ash resources/actions 与 RBAC,不重复实现业务逻辑。
- **Mock 方式**:工具测试直接调用工具模块(或经 anubis_mcp 的测试钩子),无需真实 OpenClacky。

### 2.3 ash_authentication 4.14.1(认证 + 连接 token)

- 策略:Password / ApiKey / Auth0 / Github / Google / MagicLink / Slack / OAuth2 / Oidc → **本平台用 Password(用户名+密码)+ 邀请链接**(MagicLink 作为备用体验)。
- Token:`AshAuthentication.TokenResource`(短期 JWT,`sign_in` mutation 返回);`Confirmation`(邮箱确认,邀请场景)。
- **MCP 连接 token(D6/D13)**:每用户一个连接 token(网站"连接设置"生成,绑用户、可多 Workspace、按目标资源判定租户);平台只存 hash,撤销立即失效;邀请 token 存 hash(不存明文)。
- 前端集成:GraphQL `signIn(email:, password:)` → token → cookie → `Authorization: Bearer` 头;后端 `AshAuthentication.Plug` 验证。

### 2.4 AgentRun 与工具审计(重定义,D9)

- **网站自动记录每次 MCP 工具调用**(审计流:谁 / 工具 / 参数 / 结果 / 确认 / 时间)→ `ToolCallLog`。
- **AgentRun = 按 Step 聚合的"一次干活"记录**:操作序列摘要、关联 Step、操作者;由网站根据工具调用自动生成,**v1 就做**。
- **不做用户侧上报**(上报不可靠/可篡改、连接器扩展工作量大)。
- 用户侧 OpenClacky 对话 / token 用量等执行详情**不进 AgentRun**。
- **与引擎审计的关系(D-A5)**:ToolCallLog = MCP 接入审计(谁经 MCP 调了平台什么工具);**Thread journal** = 引擎 run 的事件溯源(收到什么信号、状态如何迁移、`provenance_chain` 溯源链);AgentRun = 按 Step 聚合的领域操作记录。三者互补,对应关系见 docs/01-定稿设计/领域模型定稿.md §8。

### 2.5 前端(Next.js 16 + Apollo 4,形态 X)

- 脚手架现状:`app/apollo-provider.tsx`("use client" + ApolloProvider)、`lib/apollo-client.ts`(`createHttpLink` + `InMemoryCache`)、layout 已包裹。
- 补充:middleware/rewrite 代理;Apollo link 中间件加 Authorization。
- **页面形态(D4)**:无对话页 / 无执行页;新增连接引导页(三步 onboarding,D13/D-A7:装 OpenClacky → 装扩展 → 扩展自动配置 mcp.json)与审计查看页(v1)。

### 2.6 连接器扩展 cgc-2046(用户侧统一入口,D14)

- **命名(D14)**:扩展 id `cgc-2046`;MCP server 条目名沿用 `cgc-2046`;本地技能前缀 `cgc2046-<ws>-<skill>`(防多工作区撞名,D12)。
- **单一配置点 = mcp.json(D13/D-A7)**:扩展安装后**自动检查并安装 MCP,自动配置 mcp.json** 的 cgc-2046 条目(URL + token;D-A7 取代 D13 步骤 2 的手动粘贴片段);**网站只生成 token**;扩展只写 cgc-2046 条目、不覆盖其他条目。
- **静态内置通用"CGC 助手" agent(D10)**:用户说"用教研 Agent"→ 助手经 MCP `get_agent_instruction(workspace_id, agent_id)` 拉取网站上的 Agent 定义(prompt/skills/授权)→ 按定义工作。公共 Agent 动态创建天然支持;不做运行时下发文件。
- **工作区 Skill 同步(D11)**:工作区 Skill 定义存网站;扩展经 API handler(经 `@http_server` 访问 `@skill_loader`,与 session_manager 同模式)调 `SkillLoader#create_skill / delete_skill` 运行时写 `~/.clacky/skills/` + 立即注册(已源码验证)。**角色过滤在网站侧**(扩展只拉到该用户角色可见的技能);触发方式:扩展静态打包 `cgc-sync` skill / aside 面板按钮。

### 2.7 确认流(D8):原生 request_user_feedback

- Agent 调高风险 MCP 工具(审批/授权/邀请/策略/删除类)→ 网站**不落业务库**,建 PendingOperation → 返回 `needs_confirmation: {id, 摘要}`。
- Agent 调内置 `request_user_feedback(question, options: ["确认写入", "继续讨论"])` → 用户 OpenClacky WebUI 弹可点击卡片,Agent 停下。
- 用户点选 → 文本回传 Agent → Agent 调 `confirm(id)` → 网站**落业务库 + 审计**。
- **无 confirm 不落库**;已知风险:auto_approve 10s 倒计时自动决策(二期加冷却期)。

### 2.8 Jido 生态选型(workflow-first,D-A1–D-A6 / ADR-0002)

- **选型依据**:平台业务(报名、开课、教研、赞助、邀请)本质是多步编排,需要**一等公民的 workflow 引擎**,而非简单顺序 Step 列表;用户与 Jido/Workflow 架构研究员调研后确认 Jido 生态(Elixir)。
- **核心模型(WorkflowDefinition / WorkflowRun)**:蓝图 = Runic.Workflow DAG + 元数据(版本/类型/输入 schema/节点定义),带版本管理(改定义不影响已开始 run);实例 = 状态机 `pending → running → waiting(人等信号) → succeeded/failed/cancelled`,持输入快照/产物 facts/signal 日志,归属 partition。
- **jido_runic(DAG workflow 引擎底座)**:数据流 DAG——Step 是 `input → output` 函数,Fact 是数据流单元,lazy 并发求值(分支/并行/扇出);`react_until_satisfied/2` 执行;**ActionNode** 包装 Jido Action;**SignalMatch** 按 signal type 前缀门控下游(人工步骤/等待事件原生机制);**SignalFact** 保留溯源链(jidocause → fact ancestry);**Introspection**(provenance_chain/execution_summary)支撑审计。
- **ash_jido(Ash → Jido Action 编译期桥)**:编译期把 Ash Resource 的 action 生成 Jido Action 模块(context 需 domain/actor/tenant);业务实体 ↔ workflow 步骤的桥;依赖方向恒为 workflow → 业务 Action 接口。
- **jido_signal(CloudEvents)**:Signal 消息信封(`type` 路由、`source/subject/data` 标准化);跨 context 通信纽带——workflow 发 Signal,业务 context 订阅后更新自己的 aggregate;业务反向只发信号、不调引擎。
- **partition 多租户**:每 Workspace = 一个 partition(registry identity / persistence / lineage / telemetry 隔离);WorkflowRun 归属 partition。
- **Thread journal 审计**:append-only 事件日志 + Checkpoint(状态快照,waiting hibernate 落盘)+ Introspection 溯源链;审计 context 数据源(D-A5)。
- **Runic 0.1.0-alpha 风险**:官方实验期 → **v1 锁版本 `~> 0.1.0` + 适配层隔离**(封装 jido_runic 内部 API),未来可替换自研 DAG 或升级稳定版(ADR-0002);异步 Signal 路径需幂等键与重试策略;人工步骤需定义超时/取消语义(如报名截止后取消 run)。
- **与 BYO 不矛盾(§0/领域模型 §4.7)**:引擎只做确定性编排,不跑 LLM;需 LLM 的步骤仍由用户侧 OpenClacky 完成,经 MCP 工具/信号回引擎(D1/D3 不变)。

---

## 三、自研 RBAC 设计(核心,需写清楚)

### 3.1 数据模型

```
User(全局) ─── WorkspaceMembership(1 条/人/租户) ──── MembershipRole(N:M) ──── Role(租户内)
                                                     MembershipRole.role_id ──→ Role.id
Workspace(全局, join_policy)  Role 挂 allowed_roles(Agent 资源引用 Role.id 集合)
```

### 3.2 判定逻辑

- **多角色**:一个成员多条 MembershipRole → 权限 = 所有角色权限**并集**,命中任一即放行。
- **Step 授权**:`StepRole` 关联表(step_id, role_id);执行动作时要求成员的角色集与 Step 允许角色集**有交集**。
- **Agent 授权**:Agent 资源 `allowed_roles: [Role.id]`(公共 Agent 独立授权)。
- **MCP 调用同一授权链(D6)**:每次 MCP 工具调用 = token 认证 → 定位身份(成员关系)→ RBAC 判定 → 资源/Step 级(workspace_id 过滤)→ 执行 + 审计;**Agent 权限 = 用户本人权限,越权被拒**。

### 3.3 Ash 实现方式(不用 AshRbac)

- 每个租户资源 `policies` 里用**自定义 authorize** 或 `expression` 引用 Rbac 模块,示例:

```elixir
policies do
  policy action_type(:read) do
    authorize_if expr(workspace_id == ^actor_workspace_id)  # 租户内可见
  end

  policy action_type([:update, :destroy]) do
    authorize_if Cgc2046.Rbac.can?(actor, :manage, resource: __MODULE__)
  end
end
```

- `Cgc2046.Rbac.can?(actor, permission, opts)`:读 actor 的 memberships(membership + roles 预加载),并集展开权限集,返回布尔。
- 统一约定:**所有写操作(action)首行 `Rbac.ensure!(actor, :xxx)`**,失败返回 Forbidden;MCP 工具同样先 `Rbac.ensure!` 再执行。

---

## 四、端到端 TDD 实施计划

> 每个里程碑 = 垂直切片。**先写测试 → 实现 → 跑通 → 进入下一项**。
> 测试命令:`cd backend && mix test`;`cd web && pnpm vitest`(或 `pnpm test`)。
> 网站不跑 LLM,无需 LLM mock;MCP 工具测试直接调工具模块/anubis_mcp 测试钩子;扩展侧用 `clacky ext verify`。

### M0 地基(无 MCP,后端先行)

| # | 功能 | 测试(先写) | 实现 | 验证 |
|---|---|---|---|---|
| 0.1 | 项目接线 | `test/support` 基建 | mix.exs 加 ash/ash_postgres/ash_graphql 已含;配 `AshPostgres.Repo` data_layer;`Cgc2046.Api` + `Cgc2046.GlobalApi`(`use AshGraphql` 配 `generate_sdl_file: "priv/graphql/schema.graphql"` + `auto_generate_sdl_file?: true`) | `mix test` 绿;编译期自动生成 `priv/graphql/schema.graphql`(`use AshGraphql` `generate_sdl_file` + `auto_generate_sdl_file?`,已替代被官方移除的旧 mix 任务) |
| 0.2 | User 资源(全局)+ ash_authentication | `user_test.exs`:注册、登录、token 生成 | `User` resource(Password 策略 + TokenResource + graphql signIn) | GraphQL playground 里 signIn 返回 token |
| 0.3 | Workspace(全局, 含 join_policy) | `workspace_test.exs`:创建(仅平台管理员)、join_policy 三态 | `Workspace` resource + `is_platform_admin` 判定 | 非管理员创建被拒 |
| 0.4 | 租户资源:Role / WorkspaceMembership / MembershipRole | `membership_test.exs`:多角色并集、租户隔离 | 三个资源 + multitenancy attribute | 跨租户查不到数据 |
| 0.5 | Rbac 模块 + policies | `rbac_test.exs`:can? 判定、拒绝路径 | `Cgc2046.Rbac` + 各资源 policies | 越权 action 返回 Forbidden |
| 0.6 | JoinRequest(open/request/invite_only) | `join_request_test.exs`:三态行为、审批流 | JoinRequest 资源 + 状态 action | 三态各场景绿 |
| 0.7 | Invitation(可撤销/过期/hash token) | `invitation_test.exs`:创建、点链接校验、used/revoked/expired | Invitation 资源 + token hash + Confirmation | 各状态绿 |
| 0.8 | Profile | `profile_test.exs`:CRUD、租户内可见 | Profile 资源 | 绿 |

### M1 Workflow 引擎(workflow-first,教研场景)

> 引擎 = **WorkflowDefinition(蓝图)+ WorkflowRun(实例)**,基于 Jido 生态(D-A1/D-A2);人工步骤 workflow 内部等待为主(D-A1);本里程碑只做引擎 + 教研/学习双 workflow,业务 workflow(报名/赞助/邀请)在 M2.5。

| # | 功能 | 测试(先写) | 实现 | 验证 |
|---|---|---|---|---|
| 1.1 | WorkflowDefinition 资源 + 生命周期 | `workflow_definition_test.exs`:draft→published→archived;版本管理(改定义不影响已开始 run) | WorkflowDefinition 资源(type/version/input_schema/node_def)+ ash_state_machine | 状态流转与版本快照绿 |
| 1.2 | WorkflowRun 状态机 + 引擎执行 | `workflow_run_test.exs`:pending→running→waiting→succeeded/failed/cancelled;自动步骤执行出 facts | Jido + jido_runic 执行;WorkflowRun 资源(输入快照/facts/signal 日志);归属 partition | 状态机与产物 facts 绿 |
| 1.3 | Step 四分类 + 顺序解锁 | `step_test.exs`:自动/人工/门控/子 workflow 四类节点;Step 1 完成才能执行 Step 2 | Step 资源(type/title/agent_id/sub_definition_id);SignalMatch 门控人工步骤 | 四分类与顺序约束绿 |
| 1.4 | 人工步骤 waiting + 信号放行 | `human_step_test.exs`:执行到人工节点置 waiting;发 Signal → 放行恢复;waiting 落 checkpoint(hibernate) | SignalMatch + SignalLog + Checkpoint/thaw | 信号放行与恢复绿 |
| 1.5 | StepRole 授权 | `step_role_test.exs`:Tutor 才能做大纲设计 | StepRole 关联 + Rbac 交集判定 | 无权限执行被拒 |
| 1.6 | 教研/学习双 workflow 实例化 | `teaching_learning_test.exs`:教研 workflow 定义一次,Event/Course 实例化;学习 workflow 作为子 workflow 嵌套 | WorkflowDefinition type=research/learning;子 workflow 引用(D-A2) | 复用与嵌套绿 |

### M2 网站 MCP server + 连接器扩展(BYO,大改)

> 原 M2 为 ash_ai/ToolLoop/SSE 路线,已按 D1/D5/D6/D7/D8/D9/D12/D13/D14 整体替换。

| # | 功能 | 测试 | 实现 | 验证 |
|---|---|---|---|---|
| 2.1 | 网站 MCP server(anubis_mcp) | MCP 调用无 token → 401;错/撤销 token → 401 | 挂载 anubis_mcp;MCP endpoint + 每用户 token 认证 | `mix test` 绿;curl/OpenClacky 连上 |
| 2.2 | MCP 读工具集 | `mcp_read_test.exs`:正常读取;跨 workspace_id → 403 | `get_workspace_context / get_workflow / get_step_output / list_members / get_learner_history / get_agent_instruction`(D7/D10) | 各工具绿;越权被拒 |
| 2.3 | MCP 写工具集 | `mcp_write_test.exs`:写入成功;无权限角色 → 403 | `save_step_output / reply_learner_question`(D7) | 绿 |
| 2.4 | MCP 管理工具(低风险直做) | `mcp_manage_test.exs`:create_agent/create_workflow 直做 + RBAC 校验 | `create_agent / create_workflow` 等(D7) | 绿 |
| 2.5 | 确认流(高风险工具) | `confirm_flow_test.exs`:无 confirm 不落业务库;「确认写入」→ confirm 落库+审计;「继续讨论」→ pending 保留;仅 WebUI 明确取消 → rejected | `approve_join_request / assign_role / create_invitation / update_join_policy / 删除类` + PendingOperation + `confirm(id)`(D7/D8) | 各场景绿 |
| 2.6 | AgentRun 审计聚合 | `agent_run_test.exs`:调用序列 → ToolCallLog + 按 Step 聚合 AgentRun;无用户侧上报 | 每次工具调用自动审计;AgentRun 聚合记录(D9) | 绿 |
| 2.7 | 连接器扩展 cgc-2046 | `clacky ext verify` 绿;自动配置 mcp.json cgc-2046 条目 | 扩展骨架 + 静态内置 CGC 助手 agent + MCP 客户端(D10/D14/D-A7) | 本地联调通 |
| 2.8 | 工作区 Skill 同步 | 撞名隔离(cgc2046-<ws>- 前缀);角色过滤在网站侧 | 扩展 API handler 经 `@skill_loader` 调 `SkillLoader#create_skill/delete_skill`(D11/D12) | 多工作区不串 |
| 2.9 | Onboarding 三步 + token 签发 | 组件测试:引导页三步(装 OpenClacky → 装扩展 → 扩展自动配置 mcp.json);token 生成/撤销 | 网站连接引导页 + 连接设置页(token 绑用户不绑工作区;扩展自动配置 mcp.json,D13/D-A7) | 手动绿 |

### M2.5 业务 workflow 切片(报名/赞助/邀请,workflow-first)

> 依赖 M1 引擎 + M2 MCP。体现跨 context **同步 8 / 异步 2**(D-A6):**先做同步主链路**(核心写走 Ash Action,强一致:报名建 Enrollment、赞助建 Sponsorship),**后做异步 Signal 衍生**(通知志愿者/触发学习/权益更新,最终一致)。

| # | 功能 | 测试(先写) | 实现 | 验证 |
|---|---|---|---|---|
| 2.5.1 | 报名 workflow(同步建 Enrollment) | `enrollment_flow_test.exs`:表单提交信号 → 人工步骤放行 → 同步 `create_enrollment`(名额/唯一性)落库;重复报名被拒 | 报名 workflow 定义(人工步骤 + ash_jido 调 `create_enrollment` Action);Enrollment 资源(D-A4) | 主链路强一致绿 |
| 2.5.2 | 异步衍生 Signal | `async_signal_test.exs`:`enrollment.completed` → 通知志愿者 / 触发学习 workflow / 赞助权益更新;幂等键不重复执行 | Signal 订阅 + 幂等/重试策略(D-A6) | 异步最终一致绿 |
| 2.5.3 | 赞助 workflow(两级) | `sponsorship_flow_test.exs`:意向 → 审批(Owner/Admin)→ 权益生效;Event 级 + Workspace 级 | 赞助 workflow + Sponsorship 资源(level/tier/status)(D-A3) | 两级赞助绿 |
| 2.5.4 | 邀请 workflow(Speaker) | `speaker_flow_test.exs`:Owner 建 SpeakerInvitation → 接受/拒绝 → 分享材料产出 → 关系结束 | 邀请 workflow + SpeakerInvitation 资源(D-A3) | 邀请链路绿 |

### M3 前端页面(Next.js,形态 X)

> 无 Agent 对话页/Workflow 执行页(D4);新增连接引导/连接设置/审计查看页。

| # | 功能 | 测试 | 实现 | 验证 |
|---|---|---|---|---|
| 3.1 | rewrite 代理 + Apollo link(Authorization) | —(配置) | `next.config.ts` rewrites + link 中间件 | 浏览器登录后可查 |
| 3.2 | 登录/注册页 | 组件测试 | signIn mutation + cookie 写入 | 手动登录绿 |
| 3.3 | 公开 Club 发现页 + Club 公开主页 + 申请加入 | 组件测试 | 列表查询 + 公开主页公开信息查询(含加入入口) + JoinRequest mutation | 手动绿 |
| 3.4 | 工作台选择页 + Workspace 设置 | 组件测试 | memberships 查询、join_policy 更新 | 手动绿 |
| 3.5 | 公共 Agent 列表/详情 + Workflow 产出展示页 | 组件测试 | 查询 + AgentRun/Step 产出展示;"在 OpenClacky 中执行"引导 | 手动绿 |
| 3.6 | 连接引导页(三步 onboarding)+ 连接设置页 | 组件测试 | 三步引导(装 OpenClacky → 装扩展 → 扩展自动配置 mcp.json)+ token 签发/撤销(D-A7) | 手动绿 |
| 3.7 | 审计查看页 | 组件测试 | ToolCallLog/AgentRun 查询 + 筛选;引擎 Thread journal 溯源(只读) | 手动绿 |
| 3.8 | 成员 Profile 页 + 邀请链接生成 | 组件测试 | 查询 + Invitation mutation | 手动绿 |
| 3.9 | 平台管理后台(建 Workspace/指定 Owner) | 组件测试 | 管理 mutation(申请审批 + 主动创建两级,D-A3) | 手动绿 |
| 3.10 | Event/Course 公开页 + 报名页 | 组件测试 | Event/Course 查询(场地形态)+ 轻量报名表单 → 报名 workflow 人工步骤 | 手动绿 |
| 3.11 | 赞助发起/展示页 + 报名/赞助流程展示页 | 组件测试 | Sponsorship mutation(两级)+ WorkflowRun 状态查询(**只读展示,不执行**,形态 X) | 手动绿 |

### M4 集成验证(端到端)

- `cd backend && mix test` 全绿;`cd web && pnpm build` 通过。
- 手工端到端:注册 → 平台管理员建 Workspace(申请审批 + 主动创建两级)→ 邀请 → 分配角色 → **用户装 OpenClacky + 三步连接引导(扩展自动配置 mcp.json,D-A7)** → CGC 助手拉 Agent 指令 → 调 MCP 工具(读/写/管理/确认流)→ AgentRun 审计;Owner 建 Event/Course + 教研 workflow → Learner 报名(报名 workflow 同步建 Enrollment)→ 学习 workflow(子 workflow,Learner 侧 BYO 执行)→ 赞助 workflow(两级)→ 邀请 workflow(Speaker)→ 网站产出展示/审计查看(Thread journal 溯源)。
- 越权演练:Learner 调 `create_workflow` → 403;跨 workspace_id 访问 → 403;无 confirm 的高风险操作不落库(D8/D12);重复报名 → 名额/唯一性拒绝(2.5.1)。
- 可选 Playwright 冒烟脚本(二期)。

---

## 五、测试策略细节

- **后端**:ExUnit;Ash 资源的 `api.create!/read!`;租户隔离测试用两个 tenant 各造数据断言互不可见;**MCP 工具测试直接调工具模块/anubis_mcp 测试钩子**(网站不跑 LLM,无 LLM mock)。
- **确认流测试**:无 confirm 不落业务库;「确认写入」→ confirm 落库+审计;「继续讨论」→ pending 保留;仅 WebUI 明确取消 → rejected(D8)。
- **认证测试**:`AshAuthentication.Plug` 或用 `sign_in` mutation 拿真实 token;MCP 用每用户连接 token。
- **连接器扩展**:`clacky ext verify` 当"编译器";本地联调用 `curl` 调扩展 API + MCP 工具。
- **前端**:Vitest + @testing-library/react(组件 + hooks);页面 = 工作台/产出展示/审计/连接引导(无对话页)。
- **CI 顺序**:`mix format --check-formatted` → `mix compile --warnings-as-errors` → `mix test` → 前端 `pnpm lint && pnpm build` → 扩展 `clacky ext verify`。

---

## 六、风险与注意点

1. **AshRbac 不适用**(角色编译期)→ 自研 Rbac,注意"多角色并集"与"Step/Agent 交集"两种判定不要混。
2. **set_tenant 顺序**:放在认证 plugs **之前**,否则 User 校验拿不到租户上下文(全局资源除外)。
3. **anubis_mcp 较新**:锁定版本,关注 MCP 规范演进(tools/list、tools/call、HTTP/SSE transport);升级前看 changelog。
4. **OpenClacky MCP client 为 server 级全局长连接**(D12):不能存服务端会话状态 → 所有 MCP 工具必带 `workspace_id`,每调用鉴权 + 审计。
5. **多租户 + 全局混合**:User/Workspace 全局,其余全部租户;创建租户资源的 action 必须显式 set_tenant(从 actor 的 membership 取 workspace_id),防止跨租户写入。
6. **token 安全**:网站登录 token 存 httpOnly cookie;MCP 连接 token 绑用户不绑工作区、平台只存 hash、撤销立即失效;邀请 token 存 hash。
7. **平台零 AI 成本(BYO,D3)**:不再有网站侧 LLM 成本;用户离线则该用户 Agent 任务不可用(接受)。
8. **确认流安全(D8)**:无 confirm 不落业务库;PendingOperation 过期清理;auto_approve 10s 自动决策二期加冷却期。
9. **扩展自动配置 mcp.json(D-A7,取代 D13 步骤 2)**:单一配置点 = mcp.json;扩展只写 cgc-2046 条目、不覆盖其他条目;网站只生成 token。
10. **Runic alpha(0.1.0)**:锁版本 `~> 0.1.0` + 适配层隔离(jido_runic 内部 API),未来可替换自研 DAG 或升级稳定版(ADR-0002)。
11. **异步 Signal 幂等/重试**:衍生副作用(通知/权益/触发)需幂等键与重试策略,避免重复执行(D-A6)。
12. **人工步骤超时/取消**:WorkflowRun `waiting` 状态需定义超时与取消语义(如报名截止后取消 run)(D-A1)。
13. **引擎与业务 context 边界纪律**:依赖方向恒为 workflow → 业务 Action 接口,业务侧不得反向调引擎(ADR-0002)。

---

## 附录:依赖版本基线(锁定)

```elixir
# backend/mix.exs
{:ash, "~> 3.31"}                    # 已含
{:ash_postgres, "~> 2.11"}           # 已含
{:ash_graphql, "~> 1.10"}            # 已含
{:ash_authentication, "~> 4.14"}     # 已含
{:ash_authentication_phoenix, "~> 2.4"}  # 已含
{:anubis_mcp, "~> x.y.z"}            # 新增(网站 MCP server,版本待确认)
{:ash_state_machine, "~> 0.2.13"}    # 新增(状态机)
{:ash_archival, "~> 2.0.3"}          # 新增(软删除)
{:ash_paper_trail, "~> 0.6.0"}       # 二期(审计)
{:jido, "~> x.y.z"}                  # 新增(workflow 引擎核心,版本待确认;含 Signal/CloudEvents)
{:jido_runic, "~> 0.1.0"}            # 新增(DAG workflow 桥;Runic alpha 锁版本 + 适配层隔离)
{:ash_jido, "~> x.y.z"}              # 新增(Ash Resource → Jido Action 编译期桥,版本待确认)
# jido_signal:Jido 核心 Signal(CloudEvents),随 jido 提供或单独依赖
# 已移除:ash_ai、ash_oban(D1:网站不跑 LLM/ToolLoop)
```

```json
// web/package.json
"@apollo/client": "^4.2.9"   // 已含
"next": "16.2.12"            // 已含
// 新增(开发依赖):vitest、@testing-library/react、@testing-library/jest-dom
```

```yaml
# 连接器扩展(用户侧,非后端依赖)
id: cgc-2046            # 扩展 id(D14)
mcp 条目名: cgc-2046    # mcp.json 中的 server 条目名
本地技能前缀: cgc2046-<ws>-<skill>   # 防多工作区撞名(D12)
```







