# 发布减重降面：test/（含示例 JWT 等测试素材）与 bin/（pack 开发脚本）
# 仅随 repo 分发，不进 openclacky ext pack 产物——packager 严格遵守容器
# .gitignore（ignored 文件不入包），NEVER_PACK_DIRS 只挡 VCS/IDE 目录。
# dist/ 由仓库顶层 .gitignore 排除，不在本容器职责。
test/
bin/
