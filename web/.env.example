# web 开发环境变量（复制为 .env.local 后按需修改）
# Next.js dev server 自动读取 .env.local（不进 git）

# 后端 GraphQL 地址（rewrites destination + layout 运行时 alignUserLocale）
# 生产由部署链注入（#213 fail-fast：生产缺失即报错）；dev 显式给缺省
BACKEND_URL=http://localhost:4000

# 公开站点地址（canonical/hreflang，#254；生产为 https://codingirlsclub.com）
NEXT_PUBLIC_WEB_BASE_URL=http://localhost:3000
