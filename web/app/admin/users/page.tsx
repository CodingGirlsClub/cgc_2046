/**
 * /admin/users（Phase 7 骨架）。
 * 用户列表（R8：分页 + 搜索 + is_platform_admin + membership 概要；待 Phase 9 填充）。
 */
export default function AdminUsersPage() {
	return (
		<section>
			<h1 className="text-2xl font-semibold mb-2">用户</h1>
			<p className="text-neutral-600">
				全局用户列表与平台管理员管理（待 Phase 9 填充数据源）。
			</p>
		</section>
	);
}
