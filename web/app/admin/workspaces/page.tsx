/**
 * /admin/workspaces（Phase 7 骨架）。
 * 工作台列表 + 创建入口（待 Phase 8 填充：分页列表、搜索、pending-owner 状态）。
 */
export default function AdminWorkspacesPage() {
	return (
		<section>
			<h1 className="text-2xl font-semibold mb-2">工作台</h1>
			<p className="text-neutral-600">
				全局工作台列表与创建（待 Phase 8 填充数据源）。
			</p>
		</section>
	);
}
