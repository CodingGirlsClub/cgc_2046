/**
 * /admin/workspaces/create（Phase 7 骨架）。
 * 创建工作台（R3/R4/R5：owner 选择/邀请；待 Phase 8 填充表单 + 数据源）。
 */
export default function AdminWorkspacesCreatePage() {
	return (
		<section>
			<h1 className="text-2xl font-semibold mb-2">创建工作台</h1>
			<p className="text-neutral-600">
				创建工作台表单（名称 / slug / 加入策略 / Owner 指定；待 Phase 8 填充）。
			</p>
		</section>
	);
}
